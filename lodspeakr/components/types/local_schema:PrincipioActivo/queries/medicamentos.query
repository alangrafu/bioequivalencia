SELECT DISTINCT ?medicamento ?medicamentoLabel WHERE{
	GRAPH <http://graves.cl/bioequivalencia>{
	  ?medicamento local_schema:principioActivo <{{uri}}>;
               rdfs:label ?medicamentoLabel .
    }
}